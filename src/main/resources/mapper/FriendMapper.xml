<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.campuscrush.mapper.FriendMapper">
    <resultMap id="FriendResultMap" type="com.cc.campuscrush.entity.Friend">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="friend_id" property="friendId" />
        <result column="friend_nickname" property="friendNickname" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <select id="selectByUserIdAndFriendId" resultMap="FriendResultMap">
        select * from t_friend where user_id = #{userId} and friend_id = #{friendId}
    </select>

    <insert id="insert" parameterType="com.cc.campuscrush.entity.Friend">
        insert into t_friend (user_id, friend_id, friend_nickname, status, create_time)
        values (#{userId}, #{friendId}, #{friendNickname}, #{status}, now())
    </insert>

    <delete id="deleteByUserIdAndFriendId">
        delete from t_friend where user_id = #{userId} and friend_id = #{friendId}
    </delete>

    <select id="selectByUserId" resultMap="FriendResultMap">
        select * from t_friend where user_id = #{userId} and status = 2 order by create_time desc
    </select>

    <select id="countByUserIdAndFriendId" resultType="int">
        select count(*) from t_friend where user_id = #{userId} and friend_id = #{friendId} and status = 2
    </select>

    <select id="selectByFriendIdAndStatus" resultMap="FriendResultMap">
        select * from t_friend where friend_id = #{friendId} and status = #{status} order by create_time desc
    </select>

    <select id="selectByUserIdAndStatus" resultMap="FriendResultMap">
        select * from t_friend where user_id = #{userId} and status = #{status} order by create_time desc
    </select>

    <update id="updateStatus" parameterType="com.cc.campuscrush.entity.Friend">
        update t_friend set status = #{status} where user_id = #{userId} and friend_id = #{friendId}
    </update>

    <update id="updateFriendNickname">
        update t_friend set friend_nickname = #{friendNickname} where user_id = #{userId} and friend_id = #{friendId}
    </update>

    <select id="selectFriendNickname" resultType="java.lang.String">
        select friend_nickname from t_friend where user_id = #{userId} and friend_id = #{friendId}
    </select>
</mapper>
